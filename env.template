# ============================================
# Required Configuration
# ============================================

# OpenAI API Key (REQUIRED)
# Get from: https://platform.openai.com/api-keys
# This single key is used for:
#   - GPT translation (translator agent)
#   - Codex CLI authentication (if running locally without 'codex login')
OPENAI_API_KEY=sk-proj-...

# Repository Path (REQUIRED)
# Path to your pre-cloned Git repository
REPO_PATH=/path/to/your/repository

# ============================================
# Optional Configuration
# ============================================

# Codex Configuration
# Note: Codex CLI can use either session-based auth ('codex login') or API key (OPENAI_API_KEY)
CODEX_TIMEOUT=600
CODEX_LOGS_DIR=/Users/sean/.cbagent/codex_logs  # Raw Codex output logs (timestamped JSON files)

# Agent Configuration
# Note: TECHNICAL_AGENT_MODEL removed - Codex CLI determines model automatically
TRANSLATOR_AGENT_MODEL=gpt-5-nano

# Logging
LOG_LEVEL=INFO
LOG_FILE=/Users/sean/work/cbAgent/logs/pm_query.log

# Session Configuration
# Note: MCP_SESSION_TIMEOUT removed - MCP layer has been removed
MAX_RETRIES=3
